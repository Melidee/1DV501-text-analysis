# 1DV501 Text Analysis Project

This is my implementation of the text analysis project.
Because it implements a terminal user interface, it cannot be run as a jupyter notebook,
please open a console in jupyter and run `python3 -m text_analysis.main` from this directory to run the program
